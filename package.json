{
  "name": "kalit-app",
  "version": "0.1.0",
  "private": true,
  "packageManager": "pnpm@9.12.3",
  "scripts": {
    "dev": "pnpm --filter @kalit/landing dev",
    "build": "pnpm --filter @kalit/landing build",
    "start": "pnpm --filter @kalit/landing start",
    "lint": "pnpm -r --if-present lint",
    "typecheck": "pnpm -r --if-present typecheck"
  },
  "engines": {
    "node": ">=20"
  }
}
